# CSV by @PranavHun
A library for reading a csv into column arrays of derived Datatypes.

## Requirements
1. CMake

## Installation
`cmake -B build`

## Build
`cmake --build build`

## Usage
`csv_runner PATH`